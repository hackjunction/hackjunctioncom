{
  "host": "${process.env.HOST} || 'herokuapp.com'",
  "port": "${process.env.PORT || 5000}",
  "production": true,
  "proxy": {
    "enabled": false
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": false
  }
}
