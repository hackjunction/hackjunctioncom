{
  "routes": [
    {
      "method": "GET",
      "path": "/jobs",
      "handler": "Job.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/jobs/count",
      "handler": "Job.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/jobs/:_id",
      "handler": "Job.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/jobs",
      "handler": "Job.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/jobs/:_id",
      "handler": "Job.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/jobs/:_id",
      "handler": "Job.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
