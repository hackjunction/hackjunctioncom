{
  "name": "Hackjunction.com",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup": "npm install -g yarn && yarn install && cd backend && yarn install && cd ../frontend && yarn install && cd ..",
    "dev": "concurrently --kill-others \"npm run frontend\" \"npm run backend\"",
    "start": "node server.js",
    "backend": "cd backend && strapi start",
    "frontend": "cd frontend && yarn dev",
    "build": "cd backend && npm install && npm run setup",
    "heroku-postbuild": "cd frontend && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  },
  "author": "Juuso Lappalainen",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^4.1.0"
  },
  "engines": {
    "node": "10.7.0"
  },
  "dependencies": {
    "express": "^4.16.4",
    "path": "^0.12.7"
  }
}